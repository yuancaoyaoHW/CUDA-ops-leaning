# Benchmarks

Put small benchmark scripts here after correctness tests exist.

Benchmark rules:

- Keep input sizes explicit.
- Compare against a PyTorch reference operation.
- Record GPU name, PyTorch version, Triton version, and CUDA runtime version.
