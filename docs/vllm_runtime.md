# vLLM Runtime Notes

Use this document for notes before installing or cloning vLLM.

Focus areas:

- KV cache layout
- PagedAttention
- scheduler behavior
- continuous batching
- where custom kernels fit into the runtime

Do not install vLLM until the PyTorch and Triton baseline verification passes.
